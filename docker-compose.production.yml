services:
  app:
    image: virtigia-map-editor:${TAG:-latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      VIRTUAL_HOST: ${VIRTUAL_HOST}
      VIRTUAL_PORT: 80
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - internal
      - nginx-proxy
      - virtigia-static-databases-network

  worker:
    image: virtigia-map-editor:${TAG:-latest}
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    command: php artisan horizon
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - internal
      - virtigia-static-databases-network

  scheduler:
    image: virtigia-map-editor:${TAG:-latest}
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    command: >
      sh -lc 'while true; do php artisan schedule:run --no-interaction; sleep 60; done'
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - internal
      - virtigia-static-databases-network

  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - internal
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-p${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10

  phpmyadmin:
    image: phpmyadmin:latest
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      UPLOAD_LIMIT: 1024M
    ports:
      - "${PHPMYADMIN_PORT:-38081}:80"
    networks:
      - internal

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - internal
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  meilisearch:
    image: getmeili/meilisearch:latest
    restart: unless-stopped
    environment:
      MEILI_NO_ANALYTICS: "true"
      MEILI_MASTER_KEY: ${MEILISEARCH_KEY:-}
    volumes:
      - meilisearch_data:/meili_data
    networks:
      - internal
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://127.0.0.1:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 10

networks:
  internal:
  nginx-proxy:
    external: true
    name: nginx-proxy
  virtigia-static-databases-network:
    external: true
    name: virtigia-static-databases-network

volumes:
  mysql_data:
  redis_data:
  meilisearch_data:
  app_storage:
